# 在手湿实验私有数据目录（仓库外、机密、不入 git / 不上云）。
# 见 pcsec_pichia.wetlab_private 读取护栏 + ADR-003 / ADR-006 数据契约。
#
# 指向**仓库外**的私有区（发酵 OD / 菌株 / titer / 温度·pH 明细）。
# 未设则回退到约定的仓库同级目录 ../pcSec_wetlab_private（存在才用）；都没有 = 优雅降级。
# 护栏会拒绝任何落在仓库树内的路径，防私有数据误入 git。
PCSEC_WETLAB_PRIVATE_DIR=
